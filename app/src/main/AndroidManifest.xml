<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Almacen"
        tools:targetApi="31">
        <activity
            android:name=".feature_login.presentation.LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.Almacen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Almacen" />
        <activity
            android:name=".feature_activity.presentation.ActivityListActivity"
            android:exported="false" />
        <activity
            android:name=".feature_activity.presentation.ActivityEditorActivity"
            android:exported="false" />
        <activity android:name=".catalog.presentation.ClientListActivity" />
        <activity android:name=".catalog.presentation.StoreListActivity" />
        <activity android:name=".catalog.presentation.ReasonListActivity" />
        <activity android:name=".catalog.presentation.ArticleListActivity" />
    </application>

</manifest>